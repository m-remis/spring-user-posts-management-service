server:
  servlet:
    context-path: /api/user-posts-service
  port: 8080

application:
  name: User posts management service
  desc: Demo app for basic spring boot application
  placeholder-client:
    base-url: https://jsonplaceholder.typicode.com

developer:
  info:
    name: Michal Remis
    linked-in: https://www.linkedin.com/in/michal-remis/

spring:
  flyway:
    enabled: true
    default-schema: user_posts_management
    schemas: user_posts_management
  jpa:
    properties:
      hibernate:
        default_schema: user_posts_management
